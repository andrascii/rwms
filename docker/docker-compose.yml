services:
  rwms:
    restart: unless-stopped
    network_mode: host
    image: rwms:v0.1
    env_file:
      - .env
    privileged: true
    volumes:
      - log:/app/log
    build:
      context: .
      dockerfile: Dockerfile

volumes:
  log:
